const MAX_W = g.getWidth() - 1;
const MAX_H = g.getHeight() - 1;

const mw = Math.round(g.getWidth() / 2),
  mh = Math.round(g.getHeight() / 2);

// in px
const FONT_SIZE = 20;

/**
 * one character length in pixels
 */
const CHAR_LEN = FONT_SIZE / 2;
